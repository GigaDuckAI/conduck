1. **P1 — Unattended triggers still reach Work.** [CaptureWorkboardIntent.swift:79](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Intents/CaptureWorkboardIntent.swift:79). An automation supplying `thought` runs in `.background` and directly creates a desk card. `AddFilesToWorkIntent.swift:215` similarly publishes an envelope that drains without attendance; `ConverseIntent.swift:348` publishes supplied Work audio. The microphone path also remains: the Siri record intent opens `WorkboardVoiceCaptureView.swift:60`, whose task starts recording at line 66 without a foreground gesture. **Smallest fix:** stage unattended inputs for an explicit foreground Add/Record gesture before publication or microphone activation. Forwarding this to the intents owner does not satisfy the boundary.

2. **P2 — Phone recording can disrupt a live CarPlay capture.** [InAppAudioRecorder.swift:804](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/InAppAudioRecorder.swift:804). Microphone acquisition remains macOS-only. Starting the phone’s Work recorder during CarPlay recording reaches `AudioRecorder.swift:54`, reconfiguring their shared audio session; stopping it deactivates that session at line 141. CarPlay has no participating ownership registration. **Smallest fix:** include CarPlay and phone recording in one iOS microphone/session ownership gate, including ownership-checked release.

3. **P2 — A superseded retry can overwrite another surface’s transcript.** [InAppAudioRecorder.swift:1917](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/InAppAudioRecorder.swift:1917). A cached claim is accepted without confirmation, renewal failure is ignored at line 1955, and attachment at line 1658 checks no ownership. If its lease lapses and another surface finishes the card, the old response can overwrite the new words. CarPlay has a narrower version: its check at `CarPlayRecordingService.swift:2037` precedes attachment’s store/load/write-queue suspensions, while the actual write at `WorkVoiceCaptureCoordinator.swift:572` carries no claim. **Smallest fix:** revalidate cached claims and serialize ownership validation with the transcript commit.

4. **P2 — A failed screenshot save leaves an adoptable capture without its original owner’s reservation.** [PendingRetryStore.swift:751](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/PendingRetryStore.swift:751). Sidecar and audio writes succeed, then the screenshot write throws. The caller reports no preservation and takes no claim, but reconciliation adopts the surviving pair at line 1296. Another surface can claim it while the original path treats “nothing preserved” as permission to continue. **Smallest fix:** roll back an incomplete new arm under the lock before reporting no preservation, preserving any previously committed entry; alternatively, explicitly return and reserve partial durability.

5. **P2 — Cancellation still misses the queued transcript write.** [WorkVoiceCaptureCoordinator.swift:548](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/Workboard/WorkVoiceCaptureCoordinator.swift:548). Cancellation is checked before `await context.perform`. A Cancel Transcription gesture arriving after that check but before the closure executes still allows the transcript write at line 572 and save at line 583. **Smallest fix:** carry an observable cancellation token into the write closure and check it at commit. The new attachment-pause test stops before this window.

6. **P2 — End during microphone startup can strand the Listening modal and audio route.** [CarPlaySceneDelegate.swift:1364](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/CarPlay/CarPlaySceneDelegate.swift:1364). After `beginWorkNote` succeeds, `sessionActive` is true but `state` remains `.idle` until microphone startup commits. End during the cold settle or detector load ends the session, then assigns `.idle` again at `CarPlayRecordingService.swift:3032`. The scene observes only `state` at line 1393, so the equal assignment supplies no dismiss/deactivate signal. The abandoned startup returns without supplying one either. **Smallest fix:** explicitly drive idle presentation cleanup when End terminates a startup that has not left `.idle`.

7. **P2 — Watch’s missing-key acknowledgement still removes transcription recovery.** [AppleSpeechRelayCoordinator.swift:538](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/AppleSpeechRelayCoordinator.swift:538). Work recording publication succeeds, but a missing STT key produces a cached wordless acknowledgement. The Watch accepts it and deletes its clip; the phone creates no retry entry. Restoring the key therefore leaves a playable recording with no transcription action. The new attachment-refusal handling fixes a different branch. **Smallest fix:** establish a `.published` phone retry entry under the actual recording-card ID before acknowledging; retain wrist recovery if that preservation fails.

8. **P2 — An already-foreground phone does not discover CarPlay’s retry entry.** [ContentView.swift:1193](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/ContentView.swift:1193). CarPlay queues a failed transcription while the phone root remains foregrounded. The store posts `queueDidChangeNotification`, but its only production subscriber is the Mac’s `DictationService.swift:182`. The phone’s cached retry state remains empty until another refresh trigger occurs. **Smallest fix:** observe that notification in the phone root and invoke the existing retry-state refresh.

9. **P2 — Dismissing a failed Mac desk capture leaks the new unsaved-capture count.** [InAppAudioRecorder.swift:2110](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/InAppAudioRecorder.swift:2110). Both durable writes fail, incrementing the process-wide count. The desk sheet’s error-state Cancel merely dismisses it (`WorkboardVoiceCaptureView.swift:383`), and disappearance also skips cleanup. Subsequent quits warn about a capture whose Try Again surface is no longer reachable. **Smallest fix:** route explicit and implicit discard of that pending capture through `discardPendingWorkCapture`, balancing the count.

10. **P2 — Several strengthened guards still admit removal of their claimed protections.** [CarPlayWorkNoteTests.swift:1271](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/ConduckTests/CarPlayWorkNoteTests.swift:1271). For example, deleting `self.pendingStart == nil` from the production picker gate leaves its assignment-count assertion satisfied, allowing painting during a claimed start. The uploader’s executable tests also never invoke the final dispatch consumer: consuming its mark but returning `false` still satisfies those tests and permits an ended turn to dispatch. **Smallest fix:** pin the complete remaining short guards/consumer bodies and add outcome tests where lifecycle, commit timing, or multiple owners determine correctness. The concrete mutation assessment follows below.

11. **P3 — Quit copy misidentifies a screenshot-only loss as a recording loss.** [Localizable.xcstrings:8031](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Localizable.xcstrings:8031). `noteWorkDurability` counts a capture whose audio is safely on the desk but whose screenshot remains unsaved. The alert nevertheless says the recording has not reached the desk and quitting loses what was recorded. **Smallest fix:** use wording covering the unsaved material, such as “Something hasn’t reached Work” and “Quitting now loses what hasn’t been saved.”

12. **P3 — The QA handoff still contradicts the implemented controls and entitlement.** [handoff.md:286](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/docs/qa/work-usability/handoff.md:286). Step 77 requires removed Mute/Unmute controls and says End always saves nothing, including after publication. Long-drive recovery and competing-start checks remain absent. Decision 14 at line 99 names `carplay-communication`; the actual entitlement is `com.apple.developer.carplay-voice-based-conversation`. **Smallest fix:** apply the targeted handoff corrections specified in design §7.

The main CarPlay implementation follows the recorded design: Work remains a root action, the chooser remains gateway-only, Work has no Mute, day-one ordering is corrected, transcript parking precedes attachment, and published Work retries receive 24 hours. The chooser placement is a recorded product decision, not an unexplained deviation. I found no current path converting a Work capture into a gateway/LLM turn. The attended-entry boundary remains violated by finding 1.

The CarPlay catalog values match the implemented keys and defaults, including “Add to Work,” “Saved to Work,” and the destination-specific microphone hint. The Mac shortcuts-header rename also has a matching caller. Findings 8, 9, and 11 identify where otherwise consistent wording promises the wrong outcome.

The following mutations were derived by static reading; none was applied or executed. “Discriminates” means no surviving mutation was identified for the stated protection, not that the suite was run. These rows detail finding 10.

| Added or strengthened guard | Current discrimination, surviving mutation, and smallest correction |
|---|---|
| `CarPlayWorkNoteTests.swift:292` — parking/ownership | **Incomplete.** Wrap parking at `CarPlayRecordingService.swift:2034` in `if !capture.guardToken.audioPreserved`. Preserved captures then lose bought words on attach failure, while token/order searches survive. Pin parking as an unconditional prefix. |
| `CarPlayWorkNoteTests.swift:433` — dispatch registration lifetime | **Discriminates** for the mint/register/defer wiring, together with the production registration tests. No surviving mutation identified for the previously named missing, moved, duplicated, or no-op registration changes. |
| `CarPlayWorkNoteTests.swift:469` — chat suspension/settlement | **Incomplete.** Change `terminalizeAbandonedUserTurn` at production line 2201 to write `"sending"` instead of `"failed"`. Call-site checks survive; abandoned messages remain unresolved. Pin the helper or assert the stored outcome. |
| `CarPlayWorkNoteTests.swift:604` — no Work re-arm | **Incomplete.** Move the complete destination guard before the settle sleep in `reArmAfterSettle`. Its searched text survives, but a replacement Work session can appear during the sleep. Pin its position after the suspension. |
| `CarPlayWorkNoteTests.swift:717` — hint/day-one order | **Order discriminates** against the prior reversal/reassignment mutations. **Hint meaning does not:** change the Work hint’s English catalog value at `Localizable.xcstrings:841` to “Tap New voice chat to try again.” Key, ternary, and usage checks survive. Assert the value too. |
| `CarPlayWorkNoteTests.swift:892` — pure start gate | **`mayClaim` discriminates:** all eight combinations are covered. **`isLive` does not:** replacing the controller/scene conjunction with `(controllerAttached == sceneActive)` passes every existing fixture but admits both-false inputs. Exhaust the Boolean combinations. |
| `CarPlayWorkNoteTests.swift:969` — honest callers | **Discriminates within all three complete bodies.** The previous `claimHeld: false`, `startIsLive … || true`, and removed refresh mutations fail the current equality pins. No surviving mutation identified within those bodies. |
| `CarPlayWorkNoteTests.swift:1008` — lifecycle wiring | **Incomplete.** Delete the picker’s claim condition at `CarPlaySceneDelegate.swift:1012`, or append `|| true` to the leftover-dismiss helper’s final condition at line 678. Existing searches survive; painting or dismissal can affect another start/session. Pin the complete guards. |
| `CarPlayWorkNoteTests.swift:1311` — no Mute | **Incomplete.** Call `setMuteButton(service:)` inside `ensureVoicePresented` before presentation. The Work starter still clears buttons and contains neither forbidden call, but its callee restores Mute. Check button state at the presentation boundary. |
| `CarPlayWorkNoteTests.swift:1355` — no sticky Work mode | **Incomplete.** Make the New voice chat row handler choose `startWorkNote` when existing `lastStartDestination == .work`. All declarations and starter-prefix checks survive; after one Work note, the AI row routes to Work. Pin the row handler’s destination. |
| `CarPlayWorkNoteTests.swift:1417` — listen lineage | **Discriminates within the complete helper.** Removing its generation comparison now fails. No surviving mutation identified within that body. |
| `CarPlayWorkNoteTests.swift:1438` — both-lane preflight checks | **Incomplete.** Wrap the post-readiness guard at `CarPlayRecordingService.swift:1602` in `if workCapture != nil { … }`. Its complete text remains and the specifically forbidden old syntax is absent. An old chat refusal can again end replacement Work. Pin unconditional placement. |
| `CarPlayWorkNoteTests.swift:1491` — superseded startup | **Incomplete.** Change the captured ID at production line 736 to `listenAttemptID &+ 1`. Prefix/order checks survive, but startup never owns that ID. Also, an impossible `.recording` condition around the hand-on task preserves both searched substrings while preventing handoff. Pin the complete assignment and hand-on body. |
| `CarPlayWorkNoteTests.swift:1591` — presentation identity | **Incomplete.** Insert `presentationGeneration &= 0` before `isVoicePresented = true` at `CarPlaySceneDelegate.swift:711`. The plain-assignment regex misses it; the increment count and ordering remain correct. Presentations reuse generation 1. Reject all other writes or use unique presentation tokens. |
| `CarPlayVoiceTimingContractTests.swift:290` — stale engine cleanup | **Helper pin discriminates; cleanup scan does not.** Delete cleanup only from the stale branch at `CarPlayRecordingService.swift:884`. The scan finds identical cleanup in the later mute branch, while the stale engine remains running. Inspect the brace-matched stale arm alone. |
| `CarPlayVoiceTimingContractTests.swift:376` — reconnect cleanup | **Neighbor pin discriminates** against the old conditional teardown mutation. **Caller does not:** invert `recordingService != nil` at `CarPlaySceneDelegate.swift:150`. The cleanup call still precedes construction textually, but skips stale services. Pin the caller condition. |
| `CarPlayVoiceTimingContractTests.swift:410` — one-shot hint | **Consumption discriminates.** Production failure generation remains incomplete: wrap the callback’s lines 183–184 in `if !service.isSceneActive`. The true-assignment count survives, but foreground failures show no hint. Pin that callback’s body/outcome. |
| `CarPlayAttemptCancellationOutcomeTests.swift:123` onward — claim-set rules | **Discriminates** against token-order pruning and the reported live-claim eviction. The survivor, age, release, and outstanding-attempt controls are meaningful. No surviving mutation identified for those stated set rules. |
| `CarPlayAttemptCancellationOutcomeTests.swift:343` — production cancellation | **Deposition/registration discriminate. Consumer remains uncovered:** at `CarPlayConverseUploader.swift:499`, consume the mark and then `return false`. Tests never invoke `uploadConverse`, so they still pass while an ended turn dispatches. Pin or execute the actual consumer. |
| `PendingRetryOwnershipHandoffTests.swift:297` | **Incomplete.** Change the final `return retired` at `DictationService.swift:774` to `return true`. With failed retirement and remaining backlog, Chat handoff proceeds despite failed settlement; ordering checks survive. Pin/test the returned verdict. |
| `PendingRetrySurfaceHandoffTests.swift:437` | **Incomplete.** Wrap release at `DictationService.swift:373` in `if state == .recording`. Failed/cancelled retries retain their reservation while all required substrings survive. Pin the complete unfinished-attempt/release block. |
| `ErrorSurfaceDriftGuardTests.swift:692` — accessibility exemption | **Exemption justified; consumption unpinned.** Change the visible-name adapter at `WatchNoteView.swift:415` from `.visible` to `.spoken`. Full names then reach displayed buttons through the exempt helper. Pin the visible adapter/consumers. |
| `PendingRetryQueueTests.swift:135`, `:155` | **Discriminate** between Chat’s 600 seconds, published Work’s 86,400 seconds, and exemption. No surviving mutation identified for those stated rules. |
| `WorkVoiceRecoveryTests.swift:519` | Alone, changing the named TTL constant preserves this equality. **Companion queue tests discriminate** by pinning the numeric budget and expiry outcomes; no uncovered retention mutation identified. |
| `WorkboardVoiceScreenshotLaneTests.swift:677`, `:1602` | **Collectively discriminate** image retention, Work’s day budget, and Chat’s shorter budget. No surviving mutation identified for those stated rules. |
| `WorkboardVoiceScreenshotLaneTests.swift:59` — capture timestamps | **Discriminates** capture time from delayed write time. No surviving mutation identified for the stated timestamp fix. |
| `WorkboardVoiceScreenshotLaneTests.swift:189` — publication count | **Incomplete.** Change `releaseWorkPublication` at `InAppAudioRecorder.swift:555` to set the count to zero. Its single-recorder measurements still pass, but the first of two overlapping publications removes the second’s quit protection. Test overlapping publications. |
| `WorkboardAudioCaptureTests.swift:585`, `:660` — cancellation | **Discriminate cancellation already set before attachment, not write-boundary placement.** Moving the shared cancellation check before `ensureLoaded` still satisfies both tests. Pause inside the attachment/write scheduling window; finding 5 remains reachable. |

Prior findings re-derived against the current tree:

| Prior ID(s) | Current result | Re-derivation |
|---|---|---|
| CP-R1-01, CP-R2-01, CP-R3-01 | **Live — P1** | Background publication and intent-launched microphone paths remain; finding 1. Forwarding is task allocation, not boundary closure. |
| CP-R1-02 | **Fixed as filed** | `startConverseHop` carries the listen ID and rechecks after preparation suspensions, including before dispatch; `CarPlayRecordingService.swift:2231`, `:2427`, `:2463`. |
| CP-R1-03 | **Fixed as filed** | `dismissModalLeftOverBy` requires current service, no pending claim, and no live session; `CarPlaySceneDelegate.swift:675`. |
| CP-R1-04 | **Fixed as filed** | End and disappearance cancel the pending claim before their session operations; scene lines 1363 and 414. Finding 6 is the separate post-begin startup window. |
| CP-R1-05, CP-R2-06, CP-R3-03 | **Live — P2** | iOS still lacks shared microphone ownership; finding 2. |
| CP-R1-06 | **Fixed as filed** | Conversation notifications retain a refresh under a claim at scene line 1337; `releaseStart` drains it at line 367. |
| CP-R1-07, CP-R2-07, CP-R3-04 | **Partially fixed; P2 remains** | Attachment failures now preserve wrist recovery at `AppleSpeechRelayCoordinator.swift:924`; missing-key acknowledgements still retire it without phone recovery, finding 7. |
| CP-R1-08, CP-R2-08, CP-R3-06 | **Live — P2** | The phone still lacks the queue-change subscriber; finding 8. |
| CP-R1-09, CP-R2-12, CP-R3-10 | **Partially fixed; P2 remains** | Whole-body pins and executable registration tests close specific previous mutations. The concrete current survivors above establish remaining gaps; finding 10. |
| CP-R1-10, CP-R2-13, CP-R3-11 | **Live — P3** | Handoff step 77 and decision 14 remain inaccurate; finding 12. |
| CP-R2-02 | **Fixed as filed** | `consumeCancelClaim` removes only its own token at `CarPlayConverseUploader.swift:585`; marking no longer prunes by another token’s order. |
| CP-R2-03 | **Fixed as filed** | Compression and STT-readiness continuations both check listen lineage before either destination’s refusal; recording-service lines 1479 and 1602. |
| CP-R2-04 | **Fixed as filed** | Startup captures its generation before suspension at line 736 and rejects stale failure/commit paths at lines 832, 857, and 882; abandoned startup hands on the arming slot. |
| CP-R2-05 | **Fixed as filed** | Presentation completion checks controller and presentation generation before both success/failure mutations; scene lines 736 and 749. |
| CP-R2-09, CP-R3-05 | **Live — P2** | Cached ownership is still trusted and attachment lacks a claim-checked commit; finding 3. |
| CP-R2-10 | **Fixed as filed** | `AppDelegate.swift:363` refuses termination if the bounded wait ends with publication still outstanding. |
| CP-R2-11 | **Fixed as filed** | Abandoned appended turns and generic errors settle the exact message through `terminalizeAbandonedUserTurn`; recording-service lines 2200, 2352, and 2516. Its guard coverage remains incomplete. |
| CP-R3-02 | **Fixed as filed** | Registration spans mint to hop exit at recording-service lines 2414–2415; trimming excludes outstanding tokens at uploader line 617. The reported long-drive eviction no longer follows. |
| CP-R3-07 | **Live — P2** | Screenshot failure leaves the sidecar/audio pair recoverable without the original claim; finding 4. |
| CP-R3-08 | **Fixed as filed** | Failed preservation now records unsaved durability through `InAppAudioRecorder.swift:2022`; both quit paths consult it. Finding 9 is a new owner-lifetime defect. |
| CP-R3-09 | **Fixed as filed** | A cancellation already set on the parked-transcript path reaches the shared check at `WorkVoiceCaptureCoordinator.swift:548`, even when `settle` is skipped. The later queued-write window remains, finding 5. |