**The normal Work capture matches the founder’s intent, but the design’s “one P2 polish” verdict is incomplete.** Reviewed `a197ccd`; no files changed.

1. **[P1][DISAGREE — completeness] Esc during Ask transcription still permits a gateway send.**  
   Reproduce: record with ⌘⇧1, stop, then press Esc while transcribing. [MenuBarController.swift:736](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:736) closes through `cancelActiveCapture`, but [DictationService.swift:195](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/DictationService.swift:195) does nothing in `.processing`. The unretained transcription task continues; success invokes `onTranscript` at [DictationService.swift:877](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/DictationService.swift:877), ultimately reaching [MenuBarCoordinator.swift:2528](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarCoordinator.swift:2528).

   This contradicts the shipped “Esc always cancels the request” at [MacGeneralCategory.swift:255](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Settings/MacGeneralCategory.swift:255). Cancellation must invalidate the later handoff, even if STT cannot be interrupted. Add a test that completes STT after Esc and asserts zero gateway dispatches.

2. **[P1][DISAGREE — decision 13 and “shows the running capture”] Work can conceal the Ask recording that the next click sends.**  
   Ask’s gate checks only a live Work microphone: [MenuBarController.swift:249](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:249). Work remains active during transcription **and errors**, however: [MenuBarCoordinator.swift:1959](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarCoordinator.swift:1959). Its HUD therefore hides Ask’s recording at [DictationPopoverView.swift:352](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/DictationPopoverView.swift:352). During Work transcription, the status icon also prioritizes “Transcribing” over Ask’s live microphone: [MenuBarController.swift:865](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:865).

   A status click nevertheless stops—and sends—the hidden Ask recording at [MenuBarController.swift:197](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:197). This is an interaction defect, not merely a founder preference. Preserve Work’s pending capture while making the live recording’s identity and controls authoritative. Renaming a presentation helper cannot fix that.

3. **[P2][AGREE — decisions 4–6] The early stand-down and helper rename are appropriate for the simple conflict.**  
   Today the Work handler performs the screenshot await before rejecting a live Ask recording: [MenuBarController.swift:467](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:467), [501](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:501). An early return removes that wasted drag; keeping the later guard protects the suspension window. The existing helper only opens the popover: [MenuBarController.swift:389](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:389).

   I find no new routing regression from the proposed rename or simple guard. Text-mode refusal also already exists after the await, so decision 8 preserves an existing restriction. However, the earlier `workCaptureIsActive` branch still bypasses the new guard; finding 2 remains unresolved.

4. **[P2][DISAGREE — decision 7’s rationale] The composer-microphone query already exists.**  
   [SpeechExclusivity.swift:116](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/TTS/SpeechExclusivity.swift:116) exposes `isRecordingActive`. `InAppAudioRecorder` reports both startup and recording through that registry at [InAppAudioRecorder.swift:1912](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/InAppAudioRecorder.swift:1912).

   The draft is therefore wrong that avoiding the composer’s wasted drag requires a new lease query. A visible refusal still needs presentation, but the sentence and feedback mechanism already exist at [MenuBarCoordinator.swift:2222](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarCoordinator.swift:2222). Reconsider this deferral using the existing service; retain the actual start-time lease check.

5. **[P2][DISAGREE — decision 3’s guarantee] The two cards neither share a guaranteed timestamp nor have guaranteed adjacency.**  
   Screenshot publication receives `capture.createdAt`: [InAppAudioRecorder.swift:1666](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/InAppAudioRecorder.swift:1666). Recording publication omits it at [InAppAudioRecorder.swift:1259](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/InAppAudioRecorder.swift:1259), taking the separate `Date()` default in [WorkVoiceCaptureCoordinator.swift:169](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/Workboard/WorkVoiceCaptureCoordinator.swift:169).

   More fundamentally, each publication independently appends a sequence rank, and the desk sorts by sequence before date: [ConversationStore+Workboard.swift:1321](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:1321), [3340](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:3340). A failed picture retried after another card is added lands separately from its recording.

   Two existing card kinds are a reasonable interpretation of the intent. The claimed association needs an ordering/grouping contract and an interleaved-retry test; equal timestamps alone would not supply it.

6. **[P2][DISAGREE — completeness/HIG] Secondary-clicking during Ask recording stops and sends it.**  
   The status button accepts both mouse buttons at [MenuBarController.swift:110](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:110). The Work branch distinguishes secondary clicks, but Ask’s `.recording` branch directly toggles recording without inspecting the event: [MenuBarController.swift:175](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:175), [197](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:197).

   Thus trying to open the menu—including to choose Work—commits Ask instead. Apple describes Control-click and secondary click as context-menu gestures. [Apple HIG](https://developer.apple.com/design/human-interface-guidelines/context-menus?changes=_1) Handle that distinction consistently before stop behavior, with tests for both lanes.

7. **[P2][DISAGREE — completeness] Existing Chat recovery can send the wrong screenshot.**  
   This is already recorded as U-46, but omitted from this design’s defect inventory. Retry hands recovered words to the ordinary `onTranscript` callback at [DictationService.swift:433](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/DictationService.swift:433). That send takes the **currently staged** `pendingCaptureImage` at [MenuBarCoordinator.swift:2502](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarCoordinator.swift:2502).

   Reproduce: retain a failed Chat recording, stage another question’s screenshot, then Retry. The old words receive the new picture. Work’s separate image slot is protected, but that does not establish capture ownership within Chat. Recovery needs capture-owned attachments while preserving the separate pending composition.

8. **[P2][DISAGREE — completion claims] The typed receipt can claim arrival after import failed.**  
   `saveQuickDraftToWork` discards both the drain’s error and report, then consumes the composition and announces success: [MenuBarCoordinator.swift:2284](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarCoordinator.swift:2284), [2305](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarCoordinator.swift:2305). The drainer can throw or report refused captures: [WorkCaptureDrainer.swift:252](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/Workboard/WorkCaptureDrainer.swift:252).

   Queue acceptance preserves the capture; it does not prove a card arrived. Verify this capture’s arrival or acknowledge queueing. The existing test checks only publish→drain→banner ordering, so it misses the defect: [MenuBarWorkCaptureStateTests.swift:356](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/ConduckTests/MenuBarWorkCaptureStateTests.swift:356).

9. **[P2][DISAGREE — guard strategy] “Never inline `showPopover`” preserves a textual loophole, not a safety property.**  
   The screenshot-order guard compares the first literal `showPopover()` occurrence: [MacMenuBarWorkShortcutDriftGuardTests.swift:144](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/ConduckTests/MacMenuBarWorkShortcutDriftGuardTests.swift:144). An in-memory check confirmed the proposed helper passes while a behavior-equivalent inline refusal fails. That refusal never proceeds to a screenshot, so its early presentation cannot contaminate one.

   Keep the helper for reuse, but update the test to distinguish refusal from capture. Also, fixing finding 2 requires changing `testTheWorkHUDIsTheFirstArmOfTheContentRouter`, which explicitly pins the problematic precedence at [MenuBarWorkCaptureStateTests.swift:267](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/ConduckTests/MenuBarWorkCaptureStateTests.swift:267). The proposed new test does not cover that interaction.

10. **[P3][DISAGREE — copy/parity description; AGREE — optional plural] Several “shipped” descriptions are inaccurate.**  
    The first menu item is **Start Recording** or **Type a Message…**, not Ask: [MenuBarController.swift:1088](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:1088), confirmed by [Localizable.xcstrings:7128](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Localizable.xcstrings:7128). Settings calls that same shortcut Ask. The claim of internal naming consistency needs correction.

    “Capture to Work” versus “Add to Work” reasonably distinguishes initiation from commit; the capture ellipsis follows Apple’s additional-input convention. [Apple HIG](https://developer.apple.com/design/human-interface-guidelines/menus) “Keyboard Shortcuts” is sensible optional polish.

    Recording HUD reuse is real, but Ask transcription uses the separate `workingView`, with its ✕ hidden before answering: [DictationPopoverView.swift:1227](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/DictationPopoverView.swift:1227). Also, shortening the voice receipt to “Added to Work.” would break the test requiring “speech provider”: [MenuBarWorkCaptureStateTests.swift:659](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/ConduckTests/MenuBarWorkCaptureStateTests.swift:659).

**Verdict:** Yes, the ordinary third option captures screenshot plus voice, transcribes, and publishes to Work without a gateway turn. Explicit screenshot skipping is a reasonable extension, not a misreading. The proposed early stand-down is right, but the change list is **not complete**, and the blanket implementation verdict should be withdrawn until the interaction defects above are addressed.

All six lightweight repository guards passed. No Xcode tests or runtime UI verification were performed.

