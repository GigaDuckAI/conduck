1. **STILL OPEN as an unrestricted claim.** The reported same-owner tie is fixed: [dedupe:3179](Conduck/Conduck/Services/ConversationStore+Workboard.swift:3179) and [selector:3267](Conduck/Conduck/Services/ConversationStore+Workboard.swift:3267) use identical ordering, excluding `sequence`. However, dedupe groups by **owner + ID**, while the reader fetches by **ID alone** ([3161](Conduck/Conduck/Services/ConversationStore+Workboard.swift:3161), [3241](Conduck/Conduck/Services/ConversationStore+Workboard.swift:3241)). Cross-owner collisions remain a pre-existing exception.

2. **CLOSED.** A singleton bypasses dedupe comparison and remains the sole maximum; its projection is unchanged ([3169](Conduck/Conduck/Services/ConversationStore+Workboard.swift:3169), [3580](Conduck/Conduck/Services/ConversationStore+Workboard.swift:3580)).

3. **STILL OPEN — tied rows need not be indistinguishable.** Vault rows deliberately have **nil `contentHash`**, so equal hashes can hide different `localVaultKey` values and entirely different bytes ([1732](Conduck/Conduck/Services/ConversationStore+Workboard.swift:1732), [2983](Conduck/Conduck/Services/ConversationStore+Workboard.swift:2983)). `storageMode`, `byteSize`, thumbnails, kind, text/URL and filename can also differ; they affect availability and presentation ([projection:3555](Conduck/Conduck/Services/ConversationStore+Workboard.swift:3555), [availability:3597](Conduck/Conduck/Services/ConversationStore+Workboard.swift:3597), [name fallback:349](Conduck/Conduck/Services/Workboard/WorkboardLiveRepository.swift:349)). Raw title is tied; displayed name need not be. Smallest ordering fix: compare these distinguishing fields before `rowKey`, using thumbnail presence plus a deterministic digest for thumbnail differences.

4. **CLOSED for current callers.** Read selectors receive fresh contexts; the insertion caller selects before inserting ([contexts:1901](Conduck/Conduck/Services/ConversationStore.swift:1901), [insert:1994](Conduck/Conduck/Services/ConversationStore+Workboard.swift:1994)). No temporary ID reaches the comparison in these paths. The comment’s broader “fetch implies permanent ID” justification is incorrect: fetches can include pending inserts.

5. **CLOSED — no additional regression identified from this edit.** Test claims need qualification: the reversal test calls `max()` on constructed ordering values, without exercising either selector or actual nil/sequence fields ([Tests:1234](Conduck/ConduckTests/WorkMaterialShareTests.swift:1234)). It asserts totality for the constructed tied pair. The end-to-end board-hash versus served-byte-hash assertion is present ([Tests:1779](Conduck/ConduckTests/WorkMaterialShareTests.swift:1779)).

Clean: Read-only source review; no builds or tests run, no files changed.

