1. **P2 — Esc during screenshot acquisition can still be followed by a microphone start.** [MenuBarController.swift:452](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/MenuBarController.swift:452). After dragging closes the overlay, screenshot acquisition still awaits. Open the popover and press Esc during that wait: cancellation increments the coordinator’s token, but this handler checks only current recording flags and subsequently starts Work. **Smallest fix:** reserve and validate a cancellation generation across the entire `captureRegion` await.

2. **P2 — Screenshot failure can leave retry metadata that resurrects a deleted recording.** [InAppAudioRecorder.swift:890](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/InAppAudioRecorder.swift:890). The early retry save records `.phaseOneFailed`. Successful audio publication does not immediately correct it; silence, missing-key, or cancellation exits can leave that verdict standing. Deleting the recording and later recovering this entry republishes it. **Smallest fix:** update the armed entry to `.published` immediately after audio publication.

3. **P2 — A successfully retried screenshot can prevent retry-entry expiry indefinitely.** [PendingRetryStore.swift:1299](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/PendingRetryStore.swift:1299). Screenshot failure parks an image file. If an in-process retry publishes that image but transcription fails again, `save(workImageData: nil)` leaves the old file intact. `holdsWorkImage` then exempts the entry despite both artifacts having published. **Smallest fix:** explicitly retire the parked image under the capture’s reservation once publication succeeds.

4. **P2 — A screenshot-only failure leaves no usable retry action on Mac.** [InAppAudioRecorder.swift:1190](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/InAppAudioRecorder.swift:1190). Successful audio/transcription clears `pendingWorkCapture`; the screenshot remains queued, but the recorder returns idle and `canRetryWorkCapture` becomes false. The receipt offers only a caption. The Mac window has no pending-retry card, and the Chat retry count is not refreshed by this save. **Smallest fix:** retain screenshot debt and expose an action that retries that capture.

5. **P2 — Error copy falsely describes partial screenshot/recording outcomes.** [DictationPopoverView.swift:1027](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/DictationPopoverView.swift:1027). A screenshot can publish before audio publication fails, yet the HUD says “Nothing reached your desk.” Conversely, screenshot failure plus transcription failure says “Only the words are missing.” **Smallest fix:** use recording-specific outcome wording and include the screenshot’s omission state in the error branch.

6. **P2 — A typed receipt can inherit an earlier voice capture’s screenshot warning.** [DictationPopoverView.swift:1085](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/MenuBar/DictationPopoverView.swift:1085). After a voice screenshot omission, switch to text, save a typed note, then switch back to voice. The typed `.saved` feedback survives, as does the old recorder flag, so the reopened receipt displays the unrelated warning. **Smallest fix:** snapshot `screenshotOmitted` into the feedback value, defaulting to false for typed saves.

7. **P3 — The new Chat image-isolation test never reaches attachment construction.** [MenuBarCoordinatorQuickTypedTests.swift:418](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/ConduckTests/MenuBarCoordinatorQuickTypedTests.swift:418). Its deleted destination returns before attachments are assembled; the subsequent nil assertion runs after unconditional image cleanup. It cannot detect an image copied onto an outgoing turn. **Smallest fix:** intercept attachments for a valid isolated conversation, asserting Work-image absence and Chat-image inclusion.

Clean: ordinary overlay Esc/Return/accessibility skip; Screen Recording skip/Cancel mappings; live/start/refused-mic cancellation; screenshot-before-audio ordering and stable retry IDs; click/hotkey stop serialization and unpinning; Chat/Work image separation and aim-keyed saves; startup indicators; catalog references, retirements and `%@`; comment-resistant guards; diff whitespace. Read-only; no builds run.
