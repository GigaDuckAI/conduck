1. **MINOR — R2 #5 remains partially open.** [Change 7](</Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/docs/qa/work-usability/design/watch-work-destination.md:105>) says two root-error arms; the code has **three**. **Fix:** name both `.disabledError` branches and the gateway-refusal branch explicitly; restore QA for chooser → unavailable headless gateway → error → Dismiss.

2. **MINOR — R2 #7 needs explicit test preconditions.** [The transition tests](</Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/docs/qa/work-usability/design/watch-work-destination.md:162>) still permit a vacuous stale-hint check: entering Work from `.error` invokes `dismissError()`, which clears the hint itself. **Fix:** require `.idle` before seeding that hint; for the reverse test, await and assert `.error` plus `.work` before starting a fresh gateway request.

3. **MINOR — R1 #7’s QA closure is incomplete.** [The handoff edit list](</Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/docs/qa/work-usability/design/watch-work-destination.md:172>) omits existing step 54, which says Ask behaves “exactly as before,” and omits chooser cancellation. **Fix:** rewrite 54 as Ask → explicit gateway selection; add 0/1/N chooser-open → Cancel checks proving no capture, route push or hint mutation.

4. **NIT — R1 #3 / R2 #2 are inconsistently folded in.** [Open risks](</Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/docs/qa/work-usability/design/watch-work-destination.md:179>) still uses the founder’s largest roster, contradicting decision 2. **Fix:** repeat the maximum-supported-roster acceptance criterion, including long similar names and VoiceOver.

**Ready to implement after these small corrections; no further product decision is needed.** All other findings are closed: R1 **#1–2, #4–6, #8–11** and R2 **#1, #3–4, #6, #8–9**, including the accepted Work-last rationale. No standing-rule violations found: no new recording-state or Work capture-target case, no relay literal, no Work converse hop, `spec.md` untouched, and catalog changes listed only. Review stayed read-only; six repository guards passed, with rendering still subject to the stated QA.

