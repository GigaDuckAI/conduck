1. **STILL OPEN as blanket closure of R10 items 1–3.** Same-owner parity is fixed; R10.2’s singleton behavior remains **CLOSED**. R10.1’s pre-existing owner mismatch remains: dedupe groups by owner + ID, while single-row reads fetch by ID alone ([dedupe:3231](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:3231), [fetch:3304](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:3304)). R10.3 remains **STILL OPEN**: ordering omits `mimeType`, `caption`, and `cardSize`, although the [repository projects them](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/Workboard/WorkboardLiveRepository.swift:271). Smallest fixes: qualify parity by candidate set; compare remaining observable fields before `rowKey`.

2. **STILL OPEN — absence/totality claim is too broad.** The specified key sequence and tuple grouping are present. Optional-string encoding distinguishes nil (`""`) from every present string (`"1" + value`). However, [thumbnail hashing:307](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:307) collapses nil and empty `Data`; [construction:3359](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:3359) also collapses missing dates/title/size into `.distantPast`/`""`/`0`. `Int64` and digest-string comparisons are total; raw `Data` is never compared. Date ordering excludes NaN: [Foundation compares its underlying doubles directly](https://github.com/swiftlang/swift-foundation/blob/main/Sources/FoundationEssentials/Date.swift#L166-L177), and NaN prevents this comparator reaching `rowKey`. Smallest fix: hash present empty data, retain optional presence where promised, and qualify or enforce the valid-date domain.

3. **CLOSED — `assert` is appropriate for current callers.** No release caller passing a temporary material ID was found. Production comparisons/projections use [fresh read contexts:1913](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore.swift:1913). The insertion comparison at [2057](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:2057) precedes insertion and is test-only. The sole production constructor reads raw enum strings and is [stored eagerly:3665](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:3665). No runtime enforcement fix is presently needed.

4. **STILL OPEN — avoidable load cost; actual board latency unmeasured.** Every projected row hashes, including singletons. A read-only CommonCrypto proxy on this arm64 Mac measured **2.37 ms median / 2.42 ms p95** for 200 × 40 KB, excluding CryptoKit wrapping, hex formatting, and Core Data. Recommend capturing immutable raw thumbnail bytes in the shared constructor and memoizing their digest only after preceding keys tie. This preserves one construction path without parsed-field drift. Uncached deferred hashing is **not universally faster**: [the selector repeatedly constructs both operands:3337](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:3337).

5. **CLOSED for additional functional regressions; STILL OPEN for exact test claims.** Singleton projection remains unchanged apart from added work. The [new seam:3851](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:3851) genuinely exercises both selectors forward/reversed across six fixtures. But [the rank fixture:1944](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/ConduckTests/WorkMaterialShareTests.swift:1944) assigns 0 to an already-rank-0 card; use 1 and assert distinct ranks. The [synced-key test:1213](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/ConduckTests/WorkMaterialShareTests.swift:1213) correctly gives each tested winner the smaller row key, but never varies revision, creation date, title, or content hash. Add those four cases.

Clean: Read-only; all brief-named files reviewed; scoped diff check passed; no app builds/tests or file changes; standalone hashing proxy only.

