- **P2 — Self-links can fold through the escape candidate.** [WorkboardLiveRepository.swift:872](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/Workboard/WorkboardLiveRepository.swift:872). Given audio `C` with `attachedToMaterialID == C` and an eligible image at `escape(C)`, the resolver skips `C` but accepts the escape image. The recording folds instead of remaining standalone. Group deletion also accepts this invalid pair and deletes both materials. **Smallest fix:** reject `link == child` before examining candidates, and reject `link == childID` in [ConversationStore+Workboard.swift:3074](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:3074).
