1. **STILL OPEN — Major: revision reuse permits mismatched exports.** Store-backed gates are wired correctly, but [rollback restores the previous `updatedAt`](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Services/ConversationStore+Workboard.swift:2433). Gate 1 can read A, preparation resolve replacement B by ID, and rollback restore A before gate 2. The [revision comparison then accepts B’s copy under A’s metadata](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/WorkMaterialShareCoordinator.swift:316). **Smallest fix:** give rollback a fresh revision instead of restoring the old timestamp.

2. **CLOSED — presentation ordering and refusal handling, at source level.** [Readiness polling](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/WorkMaterialShareCoordinator.swift:407) sleeps between attempts, bounds the wait, and requires the controller’s view to belong to a window. [Presentation](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/WorkMaterialShareCoordinator.swift:463) rechecks readiness, tests the UIKit relationship, and requires a visible macOS window. The assertion about synchronous linking specifically on iOS 17/18 remains **unverified**, rather than a confirmed defect: [Apple documents completion after appearance](https://developer.apple.com/documentation/uikit/uiviewcontroller/present%28_%3Aanimated%3Acompletion%3A%29), without promising that synchronous timing.

3. **CLOSED — anchor ordering.** [Registration is idempotent; attachment promotes the view](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Components/SharePresentationAnchor.swift:72). First attachment is covered: the callback inserts missing membership, and `makeAnchorView()` also registers before returning.

4. **CLOSED — gallery preparation visibility.** [The gallery renders the shared coordinator’s preparation and failure state](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/PersonalWorkbenchView.swift:1323). The gallery itself has an opaque black background; the proposed transparent-gallery failure is not established by this source.

5. **STILL OPEN — Minor: selection tests still bypass the wiring.** [Both invocations manually supply selection values](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/ConduckTests/WorkMaterialShareTests.swift:883); the second test likewise invokes the closure directly at line 926. Neither changes the gallery’s `selection` nor evaluates its displayed action controls. Changing `currentPageID` to use `startIndex` would still leave both green. **Smallest fix:** exercise the selection binding and invoke the action produced by the rendered controls.

- **REGRESSION — Major: a throwing final store read leaks the prepared copy.** [The second `currentMaterial` call can throw](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/WorkMaterialShareCoordinator.swift:237) after preparation succeeds. The outer catch reports failure but cannot access `prepared`, so its directory survives until sweeping. **Smallest fix:** install deferred cleanup after preparation, disarming it only upon successful handoff.

- **REGRESSION — Minor: gallery failures lose their VoiceOver announcement.** [The gallery guard returns before `AccessibilityAnnouncer.announce`](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/PersonalWorkbenchView.swift:903), while the inline banner adds neither an announcement nor accessibility focus movement. **Smallest fix:** announce before selecting the visual failure surface.

Clean: context-confined store reads, ordinary metadata revision updates, first-attachment registration, original-byte copying, and per-copy aging; no confirmed readiness-spin or premature picker-release regression in current callers. Read-only review; no builds or tests run.

