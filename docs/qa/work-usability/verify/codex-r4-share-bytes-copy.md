- **Major — [PersonalWorkbenchView.swift:850](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/PersonalWorkbenchView.swift:850):** Both share validations read the cached view-model desk. Store notifications refresh it only after a 180 ms debounce, so deletion or replacement during preparation can pass the final check before that refresh. Replacement bytes can also acquire the cached material’s filename/type. **Smallest fix:** fetch and validate the current stored material before preparation and again before presentation, rechecking the request token after each await.

- **Major — [WorkMaterialShareCoordinator.swift:362](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/WorkMaterialShareCoordinator.swift:362):** Presentation follows preparation immediately, without coordinating menu dismissal or an ongoing controller transition. The presenter then returns `true` unconditionally after requesting presentation. Consequently, the required dismissal ordering and immediate reclamation of unpresented copies are not implemented. **Smallest fix:** wait for presentation readiness, revalidate the request, and confirm presentation before treating the copy as handed off.

- **Major — [PersonalWorkbenchView.swift:895](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/PersonalWorkbenchView.swift:895):** Preparing is overlaid only on the underlying shell. The gallery is a separate sheet and renders neither that banner nor another preparation indicator, so sharing a large original from the gallery lacks the required visible Preparing state. **Smallest fix:** render the coordinator’s preparation state inside the gallery sheet too.

- **Minor — [WorkMaterialShareTests.swift:711](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/ConduckTests/WorkMaterialShareTests.swift:711):** The gallery-selection test exercises only the index-to-ID helper. It never invokes `pageActions` or changes the gallery’s selection; wiring actions to `startIndex` would leave it green. **Smallest fix:** exercise a selection change through the actions slot and assert the ID received by the share callback.

Clean: byte-preserving copy/write operations; filename and UTType precedence; shared Quick Look export path; current gallery-ID wiring and Chat’s EmptyView default; card menu/accessibility gates; per-copy sweep and reclaim guards; substantive byte/coordinator/sweep tests; valid JSON, unique catalog keys and bidirectional references; absent common.share; copy-truth rules; unchanged ErrorSurfaceDriftGuard; deletion references confined to the two valid negative test fixtures.
