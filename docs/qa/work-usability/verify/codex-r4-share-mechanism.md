- **Major — [PersonalWorkbenchView.swift:850](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/PersonalWorkbenchView.swift:850):** Both Share checks read `workboardViewModel.desk`, whose refresh is debounced by 180 ms. A deletion or replacement can commit during preparation while both checks still see the old card. Byte loading independently resolves the ID from storage, so replacement bytes can also receive the old filename/MIME. **Smallest fix:** resolve current material metadata from storage before preparation and revalidate afterward, binding the exported bytes to the requested revision.

- **Major — [WorkMaterialShareCoordinator.swift:362](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/WorkMaterialShareCoordinator.swift:362):** Presentation never waits for menu dismissal. Notes and links prepare immediately; the presenter follows even a dismissing `presentedViewController`, calls `present`, and unconditionally returns `true`. Consequently, a refused presentation is treated as a handoff, suppressing failure reporting and immediate copy reclamation. **Smallest fix:** wait for dismissal/transition completion, recheck token and material, and report success only after presentation succeeds.

- **Major — [SharePresentationAnchor.swift:65](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Components/SharePresentationAnchor.swift:65):** The registry implements newest-*updated*-wins. Both representable update methods remove and reappend their view. After the gallery attaches, an ordinary update of the underlying workbench promotes its anchor above the still-attached gallery, allowing sharing from the wrong surface. **Smallest fix:** promote registrations on actual window attachment; ordinary updates must preserve attachment order.

- **Major — [PersonalWorkbenchView.swift:895](/Users/peterkruck/repos/GigaDuck/.codex/worktrees/conduck-agent-workboard/Conduck/Conduck/Views/Workboard/PersonalWorkbenchView.swift:895):** “Preparing…” is rendered only over the underlying shell. The gallery sheet supplies Share but no preparation indicator, so an expanded opaque gallery hides the banner throughout a slow export. **Smallest fix:** render the same coordinator-driven preparation state inside the gallery sheet.

Clean: source confirms supersession cleanup and Preparing reset paths; detached copy/write work and context-confined Core Data reads; original-byte copying; detached-anchor rejection and noAnchor cleanup; retained macOS picker; independent per-copy aging; unchanged Quick Look reclaim policies on both platforms; menu/accessibility Share wiring; current-page IDs and Chat’s EmptyView default; scoped catalog keys and untouched ErrorSurfaceDriftGuard.

